import{test}from'node:test';import assert from'node:assert/strict';import core from'../web/simulation.js';import{extendTown,addEconomy}from'../web/economy.js';import{addInvestors,joinPlayer,makeOffer,totalCash}from'../web/investors.js';import{skipSleepingHours}from'../web/time.js';
const town=()=>addEconomy(addInvestors(extendTown(core.createTown())));
test('quiet hours jump from 3am to 6am without financial activity; 2am is preserved',()=>{const t=town(),cash=totalCash(t);t.hour=2;assert.equal(skipSleepingHours(t),false);t.hour=3;assert.equal(skipSleepingHours(t),true);assert.equal(t.hour,6);assert.equal(totalCash(t),cash);assert.equal(t.finance.ledger.length,0);assert.equal(t.decisions,0);assert.ok(Object.values(t.businesses).every(b=>b.capital.history.length===3&&b.capital.history.every(h=>h.skipped&&h.revenue===0)));assert.equal(skipSleepingHours(t),false);});
test('unresolved human pitches and active auctions prevent the sleep jump',()=>{const t=town();joinPlayer(t);t.hour=3;const lot=makeOffer(t,'market10');t.finance.offers.push(lot);assert.equal(skipSleepingHours(t),false);lot.status='open';t.finance.active=lot.id;assert.equal(skipSleepingHours(t),false);lot.status='no-buyer';t.finance.active=null;assert.equal(skipSleepingHours(t),true);assert.equal(t.hour,6);});
